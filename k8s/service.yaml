# NodePort Service exposing the Heart Disease API outside the cluster.
# BITS Pilani AIMLCZG523 · Assignment 01
apiVersion: v1
kind: Service
metadata:
  name: heart-disease-api-service
  labels:
    app: heart-disease-api
    course: aimlczg523
spec:
  type: NodePort
  selector:
    app: heart-disease-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
